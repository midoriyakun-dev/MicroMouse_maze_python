#include<iostream>
#include<vector>
#include<queue>
#include<unordered_set>
#include<algorithm>
#include<string>
#include<utility>
#include<cmath>
#include<limits>
#include<functional>
#include<map>
#include<set>
#include<stack>


using namespace std;

int main() {

    // a basic non intelligent try and error search algorithm
    // it will try to find a path from start to end in a maze
    // it will not use any heuristics or optimizations
    // it will just try all possible paths until it finds the end
    // it will use a vector to store the path
    // it will use a queue to store the current position and the path taken to reach it
    // it will use a set to store the visited positions to avoid cycles
    // it will use a while loop to keep searching until it finds the end or exhausts all possibilities
    // I will declare a random maze for testing purposes in another header file
    // I will use a simple 2D vector to represent the maze
    // I will use a pair of integers to represent the position in the maze
    // I will use a vector of pairs to represent the path taken
    //How to randomize the maze intelligently? 
    

}